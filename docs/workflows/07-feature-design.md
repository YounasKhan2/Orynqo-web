# Workflow 07 — Feature Design
For each feature: define user goal → entry → workflow → views → components → states → edge cases → permissions → responsive behavior. Use realistic content and scale. Do not create decorative screens detached from workflows.
