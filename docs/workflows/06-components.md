# Workflow 06 — Components
Inventory existing components. Build missing primitives first, then composites, data components, and application patterns. Every component needs variants, states, keyboard/accessibility behavior, and token bindings.
