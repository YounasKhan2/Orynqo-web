# Workflow 09 — Validation
Run the design QA checklist. Compare against frozen architecture and system foundations. Log mismatches instead of silently changing the system. Stop at human review when the project workflow requires approval.
